<UserControl x:Class="ExcellCore.Module.IS.Corporate.Views.CorporateWorkspaceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Loaded="OnLoaded">
  <Grid Margin="16">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>

    <StackPanel Orientation="Horizontal">
      <TextBlock Text="{Binding Title}" FontSize="20" FontWeight="SemiBold" />
      <TextBlock Text="{Binding Subtitle}" Margin="12,0,0,0" Foreground="Gray" VerticalAlignment="Bottom" />
    </StackPanel>

    <UniformGrid Grid.Row="1" Columns="3" Margin="0,16,0,16">
      <Border Style="{StaticResource ModuleSummaryCardNeutralStyle}">
        <StackPanel>
          <TextBlock Text="Annualized Revenue" Style="{StaticResource ModuleSummaryLabelStyle}" />
          <TextBlock Text="{Binding Summary.AnnualizedRevenue, StringFormat={}{0:C0}}" Style="{StaticResource ModuleSummaryValueStyle}" />
        </StackPanel>
      </Border>
      <Border Style="{StaticResource ModuleSummaryCardWarningStyle}">
        <StackPanel>
          <TextBlock Text="Renewals Due" Style="{StaticResource ModuleSummaryLabelWarningStyle}" />
          <TextBlock Text="{Binding Summary.RenewalsDue}" Style="{StaticResource ModuleSummaryValueStyle}" />
        </StackPanel>
      </Border>
      <Border Style="{StaticResource ModuleSummaryCardSuccessStyle}" Margin="0">
        <StackPanel>
          <TextBlock Text="Allocation Risks" Style="{StaticResource ModuleSummaryLabelSuccessStyle}" />
          <TextBlock Text="{Binding Summary.AllocationRisks}" Style="{StaticResource ModuleSummaryValueStyle}" />
        </StackPanel>
      </Border>
    </UniformGrid>

    <Grid Grid.Row="2">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="3*" />
        <ColumnDefinition Width="0.5*" />
        <ColumnDefinition Width="2*" />
      </Grid.ColumnDefinitions>

      <Grid>
        <Grid.RowDefinitions>
          <RowDefinition Height="Auto" />
          <RowDefinition Height="*" />
          <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="Contract Backlog" FontWeight="SemiBold" FontSize="14" />

        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding ContractBacklog}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  IsReadOnly="True"
                  BorderBrush="#FFDDDDDD"
                  BorderThickness="1"
                  Margin="0,8,0,8">
          <DataGrid.Columns>
            <DataGridTextColumn Header="Contract" Binding="{Binding ContractCode}" Width="130" />
            <DataGridTextColumn Header="Customer" Binding="{Binding Customer}" Width="*" />
            <DataGridTextColumn Header="Value" Binding="{Binding ValueDisplay}" Width="140" />
            <DataGridTextColumn Header="Renewal" Binding="{Binding RenewalDisplay}" Width="140" />
            <DataGridTextColumn Header="Segment" Binding="{Binding Category}" Width="120" />
          </DataGrid.Columns>
        </DataGrid>

        <TextBlock Grid.Row="2" Text="{Binding StatusMessage}" Foreground="Gray" />
      </Grid>

      <Border Grid.Column="2" BorderBrush="#FFDDDDDD" BorderThickness="1" Padding="16">
        <StackPanel>
          <TextBlock Text="Allocation Watchlist" FontWeight="SemiBold" FontSize="14" />
          <ItemsControl ItemsSource="{Binding AllocationAlerts}" Margin="0,8,0,0">
            <ItemsControl.ItemTemplate>
              <DataTemplate>
                <Border Margin="0,4,0,4" Padding="12" BorderBrush="#FFE0E0E0" BorderThickness="1" Background="#FFF9F9F9">
                  <StackPanel>
                    <TextBlock Text="{Binding Program}" FontWeight="SemiBold" />
                    <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                      <TextBlock Text="Allocation" FontWeight="SemiBold" />
                      <TextBlock Text="{Binding AllocationDisplay}" Margin="6,0,0,0" />
                    </StackPanel>
                    <TextBlock Text="{Binding Status}" Foreground="#FFB26B00" />
                  </StackPanel>
                </Border>
              </DataTemplate>
            </ItemsControl.ItemTemplate>
          </ItemsControl>
        </StackPanel>
      </Border>
    </Grid>
  </Grid>
</UserControl>
